# vitest

This file is auto-generated by `npm run generate-rules-md`.

| Rule | Severity | Exceptions |
| --- | --- | --- |
| vitest/consistent-test-filename | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/consistent-test-it | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/expect-expect | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/max-expects | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/max-nested-describe | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-alias-methods | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-commented-out-tests | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-in-test | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-disabled-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-done-callback | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-duplicate-hooks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-focused-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-hooks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-identical-title | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-import-node-test | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-interpolation-in-snapshots | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-large-snapshots | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-mocks-import | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-restricted-matchers | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-restricted-vi-methods | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-standalone-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-test-prefixes | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-test-return-statement | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-called-with | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-comparison-matcher | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-each | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-equality-matcher | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-expect-assertions | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-expect-resolves | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-hooks-in-order | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-hooks-on-top | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-lowercase-title | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-mock-promise-shorthand | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-snapshot-hint | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-spy-on | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-strict-equal | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-falsy | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-object | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-truthy | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-contain | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-have-length | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-todo | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-hook | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-local-test-context-for-concurrent-snapshots | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-to-throw-message | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-top-level-describe | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-describe-callback | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-expect | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-title | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
