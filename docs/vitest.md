# vitest

This file is auto-generated by `npm run generate-rules-md`.

| Rule | Severity | Exceptions |
| --- | --- | --- |
| vitest/consistent-each-for | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/consistent-test-filename | `error` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/consistent-test-it | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/consistent-vitest-vi | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/expect-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/hoisted-apis-on-top | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/max-expects | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/max-nested-describe | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-alias-methods | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-commented-out-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-in-test | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-conditional-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-disabled-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-done-callback | `off` |  |
| vitest/no-duplicate-hooks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-focused-tests | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-hooks | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-identical-title | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-import-node-test | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-importing-vitest-globals | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-interpolation-in-snapshots | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-large-snapshots | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-mocks-import | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-restricted-matchers | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-restricted-vi-methods | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-standalone-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-test-prefixes | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-test-return-statement | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/no-unneeded-async-expect-function | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-after-all-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-after-each-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-all | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-before-all-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-before-each-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-describe-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-expect-groups | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/padding-around-test-blocks | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-called-exactly-once-with | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-called-once | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-called-times | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-called-with | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-comparison-matcher | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-describe-function-title | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-each | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-equality-matcher | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-expect-assertions | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-expect-resolves | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-expect-type-of | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-hooks-in-order | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-hooks-on-top | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-import-in-mock | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-importing-vitest-globals | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-lowercase-title | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-mock-promise-shorthand | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-mock-return-shorthand | `off` |  |
| vitest/prefer-snapshot-hint | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-spy-on | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-strict-boolean-matchers | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-strict-equal | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-falsy | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-object | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-be-truthy | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-contain | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-have-been-called-times | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-to-have-length | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-todo | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/prefer-vi-mocked | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-awaited-expect-poll | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-hook | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-local-test-context-for-concurrent-snapshots | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-mock-type-parameters | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-test-timeout | `off` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-to-throw-message | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/require-top-level-describe | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-describe-callback | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-expect | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-expect-in-promise | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/valid-title | `warn` (`**/*.test.ts`, `**/*.test.js`, `**/*.spec.ts`, `**/*.spec.js`) |  |
| vitest/warn-todo | `off` |  |
