import type { Config } from 'prettier'

export const prettierConfig: Config = {
  semi: false,
  printWidth: 100,
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 2,
  bracketSpacing: true,
}

export default prettierConfig
